import { SectionScreen } from '@/components/layout/SectionScreen';

export default function ProfileScreen() {
  return (
    <SectionScreen
      title="Perfil"
      description="Revisa la información pública de tu cuenta y accede a los ajustes."
    />
  );
}