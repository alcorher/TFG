import { SectionScreen } from '@/components/layout/SectionScreen';

export default function MyEventsScreen() {
  return (
    <SectionScreen
      title="Mis eventos"
      description="Consulta y gestiona los eventos que has publicado."
    />
  );
}