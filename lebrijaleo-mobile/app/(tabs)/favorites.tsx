import { SectionScreen } from '@/components/layout/SectionScreen';

export default function FavoritesScreen() {
  return (
    <SectionScreen
      title="Favoritos"
      description="Aquí verás los eventos que guardes para revisarlos más tarde."
    />
  );
}