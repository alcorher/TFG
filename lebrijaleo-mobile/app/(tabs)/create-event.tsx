import { SectionScreen } from '@/components/layout/SectionScreen';

export default function CreateEventScreen() {
  return (
    <SectionScreen
      title="Publicar evento"
      description="Próximamente podrás crear y publicar eventos desde aquí."
    />
  );
}